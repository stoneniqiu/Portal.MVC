@model IEnumerable<ThankNet.Models.Example>

 
@{
  
    var i = 0;
}
<ul class="slides row-fluid">
    

    @foreach (var e in Model)
    {
        
        if ((i)%4 == 0)
        {
            @Html.Raw("<li>")
           
        }
        i++;
        <div class="span3 imgbox" style="text-align: center">
            <a href="@Url.Action("Details","Example",new {id=e.Id})">
                <img src="@e.ImgUrl" />
            </a>
            
            <h6 class="carousel_header" style="text-indent: 20px;">
                <a href="@Url.Action("Details","Example",new {id=e.Id})">@e.ProjectName </a>
            </h6>
        </div>
        
         if (i%4 == 0||i==Model.Count())
        {
            @Html.Raw("</li>")
        }
    }
</ul> 
